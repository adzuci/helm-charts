This just enables the K8s auth method the same way a cluster administrator would do in a real cluster.

Intended for use in the development stack for parity with production clusters which will have already been bootstrapped.
